package amatriciana

import (
	"testing"
)

func TestBestMove(t *testing.T) {
	/*
		// mate in one for black
		position, err := PositionFromFEN("1K6/5q2/1k6/8/8/8/8/8 b - - 0 1")
		if err != nil {
			t.Fail()
		}

		bestMove := position.BestMove(2)
		expectedBestMove := Move{From: XyFromString("f7"), To: XyFromString("b7")}
		if bestMove != expectedBestMove {
			log.Fatalf("wrong move: expected %s, found %s", expectedBestMove.String(), bestMove.String())
			t.Fail()
		}
	*/
}
